.panel.panel-default
    .panel-heading
        p 商品信息管理
        a.btn.btn-primary(ng-click='goToCreate()') 添加规则
    table.table
        tr
            th 商品
            th 价格
            th 时间
            th 打折方式
            th 操作

        tr.list-item(ng-repeat='rule in rules | orderBy: "birth": reverse')
            td {{rule.description | getName}}
            td {{rule.description | getPrice}}
            td {{rule.from + '\n' + rule.to}}
            td {{'买' + rule.bought + '送' + rule.gift}}
            td
                a.item-delete(ng-click='deleteIt(promotion.id)')  删除

