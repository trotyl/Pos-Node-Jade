.container.text-center
    .panel.panel-default
        .panel-heading
            h3 商品列表
        .panel-body
            table.table.table-bordered
                tr
                    th 分类
                    th 名称
                    th 单价(元)
                    th 单位
                    th
                tr(ng-repeat='item in items')
                    td {{item.attrs | getType}}
                    td {{item.name}}
                    td {{item.price}}
                    td {{item.unit}}
                    td
                      button.btn.btn-primary.btm-sm(ng-click='buy(item.id)') 加入购物车
